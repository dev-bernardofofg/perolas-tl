# Criadas automaticamente pelo Neon Launchpad (https://neon.new) no primeiro `pnpm dev`.
# Reivindique o banco pela VITE_POSTGRES_CLAIM_URL antes de expirar (72h),
# ou substitua pelos valores do seu proprio projeto Neon.
DATABASE_URL=
DATABASE_URL_DIRECT=
